
export const serverLoader = (serverItems) => {

        serverItems.forEach(element => {
            if (element == null)
            {
                return false
            }
        })
        return true


}